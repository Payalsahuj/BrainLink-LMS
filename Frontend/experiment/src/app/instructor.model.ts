export interface Instructor {
    id?: number; // You might have an ID property if needed
    name: string;
    email: string;
    contact_number: string;
    dob: string;
    department: number; // Department ID
    gender: string;
  }
  